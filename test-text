Testing.
Testing.
1, 2, 3.

Test, 1.
Test, 2.
Test, 3.

let test = "Testing, ";

function tester(var, n) {
  let testStr = "";
  for (let i = 0; i <= n; i++) {
    testStr.push(test);
    testStr.push(num.toString(n));
    testStr.push(" ");
    }
  return testStr;
}
